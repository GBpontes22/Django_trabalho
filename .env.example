SECRET_KEY=troque-esta-chave-em-producao
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL is the database used by the course project.
DB_ENGINE=postgresql
DB_NAME=biblioteca_db
DB_USER=postgres
DB_PASSWORD=troque-esta-senha
DB_HOST=localhost
DB_PORT=5432

# For a quick local run without PostgreSQL, use:
# DB_ENGINE=sqlite
# DB_NAME=db.sqlite3
